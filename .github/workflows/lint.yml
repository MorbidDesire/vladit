name: Project lint

on:
  push:
    branches:
      - "**"
    tags:
      - "**"

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v2.3.1
        with:
          persist-credentials: false

      - name: Install 🔧
        run: make install

      - name: Run Lint 👷
        run: make lint-frontend
        
